# Machine-readable single source of truth for the Doris baseline this repo's
# FE/BE patches (doris-patches/) apply against. Read by tools/doris-baseline.sh
# and referenced by PATCHES.md. Format: KEY=VALUE, '#' comments, blank lines ok.
#
# Bump procedure + re-vendor log live in PATCHES.md (§Re-vendor log).

# Immutable fork-mirror pin. Upstream branch-catalog-spi REBASES and GCs SHAs;
# this SHA survives because it is pushed to the brikk/doris fork as an immutable
# dated baseline branch (see FORK_BRANCH / FORK_URL below).
PIN_SHA=0da96f1ad3e7b91b777195d148c921a8f23b1f96

# Upstream commit subject at the pin (match by subject if the SHA is ever GC'd
# on upstream — the fork mirror keeps the SHA alive regardless).
PIN_SUBJECT=[chore](handoff) record the 2026-07-30 rebase onto 794d514479e (upstream #65991)

# Author/commit date of the pinned commit (informational).
PIN_DATE=2026-07-30

# Immutable baseline branch on the fork mirror. Never force-pushed / rebased.
# CURRENT OPERATING MODEL: we do NOT push a fork branch. Patches are applied LOCALLY against a
# checkout already at PIN_SHA (the worktree ~/DEV/OSS/doris-catalog-spi), which is what keeps the
# SHA alive for us. The fork fields below stay for when we decide to publish a mirror.
FORK_BRANCH=duckbridge/baseline-20260730

# Fork mirror remote. Clone/fetch this (NOT apache/doris) at PIN_SHA.
FORK_URL=https://github.com/brikk/doris.git

# The upstream line this baseline tracks (informational; do not build a blind tip).
UPSTREAM_BRANCH=branch-catalog-spi
