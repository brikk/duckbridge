# Real env with the Quack token — copy from .env.example. Never committed.
.env

# Runtime copy of fe.conf, staged by smoke.sh. Doris's init_fe.sh appends
# `priority_networks` to it at boot, so it must be mutable and untracked — the tracked
# fe.conf stays pristine.
.fe.conf.runtime

# quack-jdbc driver jar staged by smoke.sh (extracted from the built plugin zip) and
# mounted into the BE. Derived artifact — never committed.
.be-drivers/
