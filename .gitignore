.gradle
build/
!gradle/wrapper/gradle-wrapper.jar
!**/src/main/**/build/
!**/src/test/**/build/

### IntelliJ IDEA ###
.idea
*.iws
*.iml
*.ipr
out/

### VS Code ###
.vscode/

# Kotlin daemon session metadata (Kotlin 2.x creates this at the project root)
.kotlin/

# Project-local Doris artifact repository — bootstrapped by tools/doris-baseline.sh
# --install-spi-jars from the pinned baseline. Never committed (multi-MB derived jars).
doris-duckbridge/doris-m2/

# Raw evidence transcript regenerated by TestQuackJdbcMetadataProbe. The distilled report
# (REPORT-quack-jdbc-metadata-probe.md) is committed; the raw dump is a derived artifact.
doris-duckbridge/dev-docs/REPORT-quack-jdbc-metadata-probe.transcript.txt
